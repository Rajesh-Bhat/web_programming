script tag in head :

whatever we write in head tag is not rendered.
script tags are handled by js interpreter


data types in js:
number
string
undefined
boolean
null

window is an object prompt is a method
DOM:doc object model

-------------------
cookies:
its a text file for storing information of client.
uniquely identify each client.
belongs to document object.
three functions:
encode
decode
escape

deleting a cookie:
set the expires property to past date